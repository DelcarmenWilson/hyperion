"use client";
import { AppointmentClient } from "@/components/lead/appointments/client";

const AppointmentsPage = () => {
  return <AppointmentClient showDate />;
};

export default AppointmentsPage;
